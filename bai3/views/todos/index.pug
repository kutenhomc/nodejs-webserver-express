h1 Hello CodersX 111
a(href="/todos") See todos list
form(action="todos", method="GET")
    input(type="text", placehoder="Search Name", autocomplete="on", name="q")
    button Search
ul
    each todo in todos
        li= todo.name